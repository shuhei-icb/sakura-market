.col-md-6.offset-md-3.mt-5
  .card.p-3
    %h5.card-title.text-center 新規会員登録
    = simple_form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f|
      = f.input :name, required: true, autofocus: true, input_html: { autocomplete: 'name' }
      = f.input :email, required: true, input_html: { autocomplete: 'email' }
      = f.input :password, required: true, hint: ("#{@minimum_password_length} 文字以上" if @minimum_password_length)
      = f.input :password_confirmation, required: true
      = f.input :address, required: true, input_html: { autocomplete: 'address' }

      .text-center= f.button :submit, class: 'btn btn-accent'
